# Railway PostgreSQL Kal?c? Veritaban? Ayarlar?
#
# GÜVENL?K DÜZELTMES?: ?ifre art?k burada düz metin olarak YAZILMIYOR.
# ${SPRING_DATASOURCE_PASSWORD} ifadesi, uygulaman?n çal??t??? ortamdaki
# (Railway'in "Variables" sekmesindeki) bir ortam de?i?keninden okunaca??
# anlam?na gelir. Bu dosya (application.properties) git'e (GitHub'a) push
# edilse bile art?k içinde gerçek ?ifre olmayaca?? için risk olu?turmaz.
#
# YAPMAN GEREKEN: Railway projendeki uygulama servisinin (Postgres'in kendisi
# de?il, senin "Mentor" servisin) "Variables" sekmesine git ve ?u de?i?keni ekle:
#   SPRING_DATASOURCE_PASSWORD = (Railway'de yeniden olu?turdu?un yeni ?ifre)
#
# Not: Railway'de ayn? proje içinde bir Postgres eklentisi varsa, Railway genelde
# ba?lant? bilgilerini otomatik olarak ortam de?i?keni ?eklinde (ör. DATABASE_URL,
# PGPASSWORD gibi) zaten sa?lar. Postgres servisinin "Variables" sekmesine bak?p
# oradaki haz?r de?i?kenleri de kullanabilirsin; a?a??daki isimlere ba?l? kalman
# ?art de?il, sadece application.properties içindeki ${...} isimleriyle Railway
# panelindeki isimlerin birebir e?le?ti?inden emin ol.

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres.railway.internal:5432/railway}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Oturumlar? (Giri?leri) RAM yerine Veritaban?nda Kal?c? Tutma Ayar?
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
